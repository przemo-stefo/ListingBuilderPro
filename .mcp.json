{
  "mcpServers": {
    "MCP_DOCKER": {
      "command": "docker",
      "args": ["mcp", "gateway", "run"],
      "type": "stdio"
    },
    "google-ads": {
      "command": "npx",
      "args": ["-y", "@trueclicks/google-ads-mcp-js", "--token=${GAQL_TOKEN}"],
      "type": "stdio"
    }
  }
}